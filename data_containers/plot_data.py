class PlotData(object):
    def __init__(self, title, labels, results, data_ranking, xlabel, ylabel):
        self.title = title
        self.labels = labels
        self.results = results
        self.data_ranking = data_ranking
        self.xlabel = xlabel
        self.ylabel = ylabel